

 pipeline {
    agent { label 'k3s' }

stages {
        stage('Compilation') {
            steps {
                script {
                    sh '''
                        pwd
                        chmod +x ./build.sh
                        ./build.sh no
                    '''
                }
            }
        }
}
}
